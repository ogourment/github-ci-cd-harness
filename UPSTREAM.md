# Upstream provenance

This repository was imported with its complete Git history from:

```text
https://framagit.org/olivierg/gitlab-ci-cd-harness
```

The original project remains the source for the GitLab-specific templates.
GitHub-native work is developed here. When code can remain platform-neutral,
changes should be designed so they can be shared in both directions.
