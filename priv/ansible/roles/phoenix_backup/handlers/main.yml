---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart backup timer
  ansible.builtin.systemd:
    name: "{{ backup_timer_unit }}"
    state: restarted
    enabled: true
    daemon_reload: true
